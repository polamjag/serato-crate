require 'spec_helper'

describe Serato::Crate do
  it 'has a version number' do
    expect(Serato::Crate::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
